// Placeholder para Fase 1 — FullPlayerSheet com @gorhom/bottom-sheet + reanimated
// Será implementado na Fase 1 com:
// - BottomSheetModal (snapPoints: ['60%', '95%'])
// - Slider de progresso (currentTime/duration via useAudioPlayerStatus)
// - Controles: shuffle, prev, play/pause, next, repeat
// - Artwork animado 40->240 via useAnimatedStyle

import { View, Text } from "react-native";

export function FullPlayerSheet() {
  return (
    <View>
      <Text>FullPlayerSheet — placeholder Fase 0</Text>
    </View>
  );
}
